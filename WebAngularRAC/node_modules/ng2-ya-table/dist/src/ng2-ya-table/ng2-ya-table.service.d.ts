/// <reference types="es6-shim" />
import { Observable } from 'rxjs/Observable';
import { ITableOptions, ITablePaging, ITableColumn, ColumnState, PagingState } from './ng2-ya-table-interfaces';
export declare class Ng2YaTableService {
    private stateChangedSource;
    stateChanged$: Observable<Ng2YaTableService>;
    showFilterRow: boolean;
    orderMulti: boolean;
    columns: ColumnState[];
    paging: PagingState;
    sortStack: ColumnState[];
    fullTextFilter: string;
    language: any;
    constructor();
    setOptions(options: ITableOptions): void;
    setColumns(columns: Array<ITableColumn>): void;
    setPaging(paging: ITablePaging): void;
    toggleSort(colState: ColumnState, orderMulti: boolean): void;
    changePaging(page: number, itemsPerPage: number): void;
    changeFilter(column: ColumnState): void;
    notify(): void;
}
