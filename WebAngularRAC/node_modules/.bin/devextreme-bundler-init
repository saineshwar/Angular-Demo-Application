#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../devextreme/bin/bundler-init.js" "$@"
  ret=$?
else 
  node  "$basedir/../devextreme/bin/bundler-init.js" "$@"
  ret=$?
fi
exit $ret
