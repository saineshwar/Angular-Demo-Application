/// <reference types="es6-shim" />
import { OnInit, OnChanges, OnDestroy, SimpleChanges } from '@angular/core';
import { DomSanitizer, SafeHtml } from '@angular/platform-browser';
import 'rxjs/add/operator/debounceTime';
import { ITableDataSource, ITableOptions, ITableColumn, ITablePaging } from './ng2-ya-table-interfaces';
import { Ng2YaTableService } from './ng2-ya-table.service';
export declare class Ng2YaTableComponent implements OnChanges, OnDestroy, OnInit {
    private sanitizer;
    private state;
    private subscription;
    private fullTextFilterValueChanged;
    processing: boolean;
    options: ITableOptions;
    rows: Array<any> | ITableDataSource;
    datasource: ITableDataSource;
    columns: Array<ITableColumn>;
    paging: ITablePaging;
    constructor(sanitizer: DomSanitizer, state: Ng2YaTableService);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
    sanitize(html: string): SafeHtml;
    onChangeTable(): void;
    getHtml(row: any, column: ITableColumn): string;
    getData(row: any, propertyName: string): string;
    cellClick(row: any, column: ITableColumn): void;
    onFullTextFilterValueChange(event: any): void;
}
